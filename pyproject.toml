[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neuro-paper-digest"
version = "0.6.1"
description = "Neurofeed literature, Bluesky network, transparent ranking, feedback learning, and newsletter delivery backend"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.32,<3",
  "PyYAML>=6.0,<7",
  "rapidfuzz>=3.9,<4",
  "beautifulsoup4>=4.12,<5",
  "python-dateutil>=2.9,<3",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[project.scripts]
neurofeed-sync-literature = "neuro_digest.jobs.sync_literature:main"
neurofeed-sync-follows = "neuro_digest.jobs.sync_user_follow_graphs:main"
neurofeed-sync-bluesky = "neuro_digest.jobs.sync_bluesky_accounts:main"
neurofeed-resolve-social = "neuro_digest.jobs.resolve_social_papers:main"
neurofeed-embed = "neuro_digest.jobs.embed_new_papers:main"
neurofeed-embed-users = "neuro_digest.jobs.embed_user_profiles:main"
neurofeed-refresh-feedback = "neuro_digest.jobs.refresh_feedback_profiles:main"
neurofeed-rank-user = "neuro_digest.jobs.rank_user:main"
neurofeed-generate-digests = "neuro_digest.jobs.generate_weekly_digests:main"
neurofeed-send-digests = "neuro_digest.jobs.send_weekly_digests:main"

[tool.setuptools.packages.find]
where = ["src"]
